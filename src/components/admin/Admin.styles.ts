import styled from "styled-components";

export const AdminWrapper = styled.div`
  display: flex;
  flex-direction: column;
  max-width: 500px;
  gap: 25px;
`;

export const BookUploader = styled.img`
  width: 24px;
  height: 24px;
  cursor: pointer;
`;
